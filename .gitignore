# Directorios de módulos de Node.js
node_modules/
frontend/node_modules/
backend/node_modules/

# Directorios de compilación/salida
build/
dist/
.cache/
frontend/build/
frontend/dist/
backend/build/
backend/dist/

# Archivos de log
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos generados por el sistema operativo
.DS_Store
Thumbs.db

# Archivos de IDE y editor (VS Code, IntelliJ, Sublime Text, etc.)
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Variables de entorno
.env
.env.local
.env.*.local
*.env

# Caché de TypeScript
tsconfig.tsbuildinfo

# Archivos de base de datos (¡IMPORTANTE: NUNCA SUBAS DATOS REALES DE DB!)
# Solo sube archivos de esquema (.sql), migraciones, o scripts de setup.
# Ignora archivos de datos, backups, o dumps.
*.sqlite
*.sqlite3
*.db
*.bak
*.dump
*.sql.gz
*.tar.gz
/database/*.db # Si tu archivo db está directamente en la carpeta database
/db/*.db       # Si tu archivo db está directamente en la carpeta db
data/
backup/
dumps/

# Archivos de socket de MySQL (específicos de Unix, ignorar en Windows)
mysql_data/mysql.sock # ¡Esta línea es CRÍTICA y debe estar!

# Python virtual environments (si tu backend es Python)
venv/
.venv/
__pycache__/
*.pyc
*.pyd
*.so

# Otros archivos temporales o generados
*.tmp
*.temp
*.swp
